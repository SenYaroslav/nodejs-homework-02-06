const { validationSchema } = require("./validationSchema");
const { userSingUpSchema } = require("./userSingUpSchema");
const { userSingInSchema } = require("./userSingInSchema");

module.exports = {
  validationSchema,
  userSingUpSchema,
  userSingInSchema,
};
